import { InjectionToken } from "@angular/core";

// Injection token.
export const lookupListToken = new InjectionToken('lookupListToken')

export const lookupLists = {
  mediums: ['Movies', 'Series', 'Animation']
};
